
// main thread of execution 

// IMPORTS
const get = require('./xget');
const uri = require('./uri');

//  EXECUTION of functions here
console.log(uri.uri_one_(0));
//[0].elements);

// uri_ get_ element_  json_
//get.aData_(uri.uri_()[0].url)
//.then(x => get.asDom_(x,uri.uri_()[0].elements[1].filter2));
